# Runtime + test dependencies for the API. Pinned to what `apienv/` actually runs
# (verified against pip show, 2026-08-26), so a fresh venv reproduces it:
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Direct dependencies only — transitives resolve on their own. `pip freeze` in
# apienv lists 259 packages, most of them pulled in by these 17.

fastapi==0.115.11
uvicorn==0.34.0
python-multipart==0.0.20
jinja2==3.1.6

# Mongo (async driver + the sync one the capture script uses)
motor==3.7.0
pymongo==4.11.3

redis==5.2.1

pydantic==2.10.6
pydantic-settings==2.8.1
python-dotenv==1.0.1

# Auth
passlib==1.7.4
bcrypt==4.3.0
pyjwt==2.10.1

# Scheduled price monitoring
apscheduler==3.11.0

# Transactional email (price alerts)
sib-api-v3-sdk==7.6.0

requests==2.32.3

pytest==9.0.2
